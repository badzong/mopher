CFLAGS=-Wall -g -shared -nostartfiles -fPIC -I../../include
OBJECTS=greylist.so
ACFILES=Makefile

all: $(OBJECTS)

greylist.so: greylist.c Makefile
	$(CC) $(CFLAGS) -o greylist.so greylist.c

clean:
	-rm $(OBJECTS)

distclean:
	-rm -f $(ACFILES)

